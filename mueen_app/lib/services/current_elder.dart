// lib/services/current_elder.dart
//
// متغير عالمي يحمل بيانات كبير السن المسجّل حالياً.
// يُضبط عند نجاح تسجيل الدخول ويُمسح عند تسجيل الخروج.

import '../models/elder.dart';

/// كبير السن المسجّل حالياً في التطبيق.
/// يكون null إذا لم يسجّل أي كبير سن دخوله بعد.
Elder? currentElder;
